# Computer protocol

Private schemas shared by Synergy's native Computer tools and Desktop host. The host maps bounded window operations to Cua; this package does not execute input or decide permissions.

Run `bun run typecheck`, `bun run build`, and `bun run test`.
