# Synergy Oryn Acceptance Record (A01–A16)

Per-item acceptance for the Oryn feedback-to-PR implementation against the criteria table in [the proposal](../decisions/proposed/architecture/2026-09-07-synergy-oryn.md). `blocked` never counts as `pass`; each blocked item lists its minimal environment dependency. Evidence references behavior tests under `packages/synergy/test/oryn/` (49 tests, all green) plus the named suites.

Local gate evidence (all green on this branch): `bun test test/oryn/` (49), migration registry suite (13 domains), `test/tool/bash-github-token.test.ts` (5, worker credential stripping), `test/channel/provider/github/` + `test/tool/github-deliver-fix.test.ts` (65), boss/agent suites (153), app production-build contract (2), ui suite (4), `coverage:check` (all 11 packages PASS; synergy 75.3% lines / 79.7% functions), `format:check`, `doc:check`, `decision:check`, `test-layout:check`, `localization:check`, `i18n:check`, and `script/generate.ts` run twice with zero drift.

| 编号 | 行为验收    | 结果                                 | 证据与命令                                                                                                                                                                                                                                                                                                                                                   | blocked 最小环境依赖                                                                       |
| ---- | ----------- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------ |
| A01  | 问答与受理  | 逻辑 pass；真实飞书联调 blocked      | QA prompt 指示直接回答、澄清优先、不虚报受理（`src/agent/prompt/oryn/qa.txt`）；受理幂等与路由测试 `bun test test/oryn/service.test.ts`                                                                                                                                                                                                                      | 飞书测试 App（app id/secret 占位）+ 一个群话题做真实问答/受理回放                          |
| A02  | 来源隔离    | pass                                 | 每 session Host 绑定 sourceKey；跨源访问拒绝测试（`getCaseForSource` ACL、non-QA 提交拒绝、get/list 过滤）——`bun test test/oryn/`                                                                                                                                                                                                                            | —                                                                                          |
| A03  | 幂等与恢复  | pass（逻辑面）                       | claim-first 受体：`claimSource` 锁 + requestKey 重放去重；`incompleteClaims` 为恢复输入；`session_source` Host-owned 绑定辨认孤儿。同上命令                                                                                                                                                                                                                  | —                                                                                          |
| A04  | 调度并发    | pass（逻辑面）                       | dispatch requestKey 去重 + BossService 异步 spawn 不阻塞派单（service.test）；executor 并发车道与 case 预算（executor tests）                                                                                                                                                                                                                                | —                                                                                          |
| A05  | 文件与凭据  | 部分 pass；完整沙箱 blocked          | worker 凭据剥离 5 项回归（gh/SSH 注入变量剥离、curl/脚本路径取不到 token、其他会话不受影响）`bun test test/tool/bash-github-token.test.ts`；code worktree 钉定 attempt 基线                                                                                                                                                                                  | Linux + 独立 UID + `unshare`/seccomp/cgroup 可用，验证 `requiredCapabilities` 真实强制     |
| A06  | 复现真实性  | pass（逻辑面）                       | executor：baseline 红 / candidate 绿；allowlist 外命令拒绝；timeout → inconclusive（infrastructureFailure）——`bun test test/oryn/service.test.ts`（checks and executor 组）                                                                                                                                                                                  | —                                                                                          |
| A07  | 版本一致    | pass                                 | 陈旧 epoch 报告只归档不推进（service.test）；review 钉定 frozen candidate 与 baseline（review.test）；rework 后旧 attempt `superseded`、证据不跨 attempt 继承（`rotateAttempt`）                                                                                                                                                                             | —                                                                                          |
| A08  | 独立 review | pass                                 | 工程 root 不能自审（NOT_AUTHORIZED）；head/base 不符拒绝；prior finding 不得静默消失（EVIDENCE_INSUFFICIENT）；reviewer 建议与 P0/P1 blocker 进 delivery gate（review.test）                                                                                                                                                                                 | —                                                                                          |
| A09  | 有界执行    | pass                                 | 3/2 轮上限确定性转 `human_owned`（review.test 两个 cap 用例）；no-progress 以 candidateSha 未变判定；case 级时间/重跑预算不因新 attempt 重置（executor `checkBudget`）                                                                                                                                                                                       | —                                                                                          |
| A10  | 发布防重    | 逻辑 pass；真实 GitHub 闭环 blocked  | publish.test：issue 重放幂等、draft PR 去重（远端事实核对）、超时/中断 → ambiguous → 有界对账 settle、不确定 → pause 不重放（`bun test test/oryn/publish.test.ts`）                                                                                                                                                                                          | GitHub App（Contents/Issues/PRs/Statuses/Checks 最小权限）+ 测试 repo 走真实 push/issue/PR |
| A11  | 远端竞争    | 逻辑 pass；真实 GitHub blocked       | takeover/cancel bump epoch → in-flight action cancelled；人工移动分支 → cancel + pause（fail-closed）；non-fast-forward → `rejected` 永不重放（publish.test）                                                                                                                                                                                                | 同 A10                                                                                     |
| A12  | 静默通知    | pass（逻辑面）；真实飞书投递 blocked | outbox 按 (case, kind) dedup；`ready` 一次性投递（dedupKey 崩溃安全）；kind 枚举限定六类结果，过程噪声无投递路径（service.test reply outbox 组、publish.test mark_ready outbox）                                                                                                                                                                             | 飞书测试 App 真实投递验证                                                                  |
| A13  | 权限与保护  | pass                                 | 无 merge/release 代码路径（transport 仅五类受控操作，notify 为 no-op）；marker + `authorIsApp` 校验拒绝伪造产物（publish.test）；`enforceOrynCeiling` 权限天花板（agent 套件）；敏感读 deny 规则（builtin-oryn clamp）                                                                                                                                       | —                                                                                          |
| A14  | 环境与降级  | 部分 pass；真实 preflight blocked    | `requiredCapabilities` 声明式校验 + `ENVIRONMENT_UNAVAILABLE` fail-closed 路径（executor/config 逻辑）；无隔离环境转人工路径存在                                                                                                                                                                                                                             | Linux VPS 部署实测 preflight（capability 探测 + 降级路径）                                 |
| A15  | Library     | pass                                 | learn.test：证据 ref 必须是本 case 真实记录；`verifiedMemory` 默认关（门控双闸：需 delivered attempt）；提升为独立自足条目、withdraw 精确移除自身条目；`autoReward` 因上游 reward API 缺事件幂等而按 proposal 保持未实现                                                                                                                                     | —                                                                                          |
| A16  | 升级与开关  | pass                                 | oryn 域迁移注册（migration registry 枚举 13 域）；fresh/upgrade/重复运行经 central runner（migration 套件）；中断恢复由 claim/orphan 逻辑覆盖；disabled 零变化：`bun test test/oryn/server-oryn-route.test.ts`（disabled 拒绝 control 写、list 返回空）+ 工具执行期 enabled 校验。残余：Scope dispose/rebind 全矩阵未单独注入测试（Host 行为，列为已知限制） | —                                                                                          |

## 已知限制与待环境验收汇总

- 真实飞书闭环（A01/A12 投递面）、真实 GitHub App 闭环（A10/A11 远端面）、Linux VPS preflight（A05/A14 沙箱面）为三项待环境验收；其余各项均有本地行为测试证据。
- Scope dispose/rebind 场景未单独注入测试（A16 残余）；Host 侧 fail-closed（孤儿绑定视为未绑定）。
- `workflow:check` 门禁在本环境因 actionlint 二进制下载被网络策略阻断而无法本地运行（未修改任何 workflow 文件）；其余门禁全绿。
- 最终门禁阶段发现并修复两处基线回归：产品清单中 `registerSkillDomain` 调用被 `registerOrynDomain` 插入时挤掉（已恢复），server 挂载 `/asset` 被 `/oryn` 顶替（已恢复并重生成 SDK）。
